CREATE OR REPLACE PACKAGE BODY my_package AS
    PROCEDURE say_hello(p_name IN VARCHAR2) IS
    BEGIN
        DBMS_OUTPUT.PUT_LINE('Hello ' || p_name);
    END;

    funtion get_data(input in number) 
    return number
    is
    begin
    selevct into variable ;
    end;
END my_package;
/
